# SMTP (cPanel / mail.shipgen.net)
SMTP_HOST=mail.shipgen.net
SMTP_PORT=465
SMTP_USER=sales@shipgen.net
SMTP_PASS=your-email-password-here

# Addresses
SALES_EMAIL=sales@shipgen.net
NOREPLY_EMAIL=noreply@shipgen.net

# Branding (email templates)
SITE_URL=https://shipgen.net
BRAND_NAME=ShipGen
# EMAIL_LOGO_URL=https://shipgen.net/logo_logistic.png

# API server
CONTACT_API_PORT=3001
ALLOWED_ORIGINS=http://localhost:5175,http://localhost:4173

# Frontend (optional — defaults to /api/contact via Vite proxy in dev)
# VITE_CONTACT_API_URL=https://your-api-domain.com/api/contact
